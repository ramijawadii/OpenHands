/**
 * Adapter: UI + utility re-exports for the copy-standalone tool-ui components,
 * repointed at this project's paths (`#/` alias, `cn` from utils/utils).
 */
export { cn } from "#/utils/utils";
export { Button } from "#/components/ui/button";
export { Collapsible, CollapsibleTrigger } from "#/components/ui/collapsible";
